<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top navbar-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><i class="fa-regular fa-compass"></i> WanderEase</a>
    <!-- Desktop nav (md and up) -->
    <div class="d-none d-md-flex w-100">
      <div class="navbar-nav me-auto">
        <a class="nav-link" href="/">Home</a>
        <a class="nav-link" href="/listings">ExplorePlaces</a>
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <a class="nav-link" href="/listings/new">Add new Listing</a>
        <% } %>
      </div>
      <div class="navbar-nav">
        <% if (typeof currentUser !== 'undefined' && currentUser) { %>
          <a class="nav-link" href="/profile">My Profile</a>
          <a class="nav-link" href="/logout">Logout</a>
        <% } else { %>
          <a class="nav-link" href="/login">Login</a>
          <a class="nav-link" href="/signup">Sign Up</a>
        <% } %>
      </div>
    </div>

    <!-- Mobile toggler -->
    <button class="navbar-toggler d-md-none border-0" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileNav" aria-controls="mobileNav" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Mobile offcanvas menu -->
    <div class="offcanvas offcanvas-start d-md-none" tabindex="-1" id="mobileNav" aria-labelledby="mobileNavLabel">
      <div class="offcanvas-header">
        <h5 class="offcanvas-title" id="mobileNavLabel">Menu</h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
      </div>
      <div class="offcanvas-body">
        <div class="navbar-nav">
          <a class="nav-link" href="/">Home</a>
          <a class="nav-link" href="/listings">ExplorePlaces</a>
          <% if (typeof currentUser !== 'undefined' && currentUser) { %>
            <a class="nav-link" href="/listings/new">Add new Listing</a>
          <% } %>
          <hr>
          <% if (typeof currentUser !== 'undefined' && currentUser) { %>
            <a class="nav-link" href="/profile">My Profile</a>
            <a class="nav-link" href="/logout">Logout</a>
          <% } else { %>
            <a class="nav-link" href="/login">Login</a>
            <a class="nav-link" href="/signup">Sign Up</a>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</nav>